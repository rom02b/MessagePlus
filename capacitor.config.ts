import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.romainbiava.messageplus',
  appName: 'MessagePlus',
  webDir: 'dist'
};

export default config;
